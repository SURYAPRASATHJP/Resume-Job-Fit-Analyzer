# Copy to .env and adjust as needed.

# Ollama (local LLM) settings for the GenAI critique component.
OLLAMA_MODEL=llama3.1:8b
OLLAMA_HOST=http://localhost:11434

# Path to the trained classifier pipeline (created by `python src/train.py`).
MODEL_PATH=models/pipeline.joblib
