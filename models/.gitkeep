Trained model artifacts are written here by `python src/train.py`.
The fitted pipeline (models/pipeline.joblib) is generated locally and is not
committed to the repo — train it yourself after adding the dataset.
